

namespace ISC_WebTracer
{
    internal class Cache
    {
        /*
         * Not sure if this is a long term good idea but in the
         * short term it prints the variables that a user can include
         * long term if I add to many user inputs[0] might be a mess
         */
        public String[] inputs = ["ISC_PORT", "ISC_ALLOWED_SITES", "ISC_REDIRECT", "ISC_API_KEY"];
        public Cache() { 
            
        }
        
        
    }
}
